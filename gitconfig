[user]
	email = samuelk373@gmail.com
	name = Samuel Desmond Kurtzer
[core]
	editor = vim
[diff]
	tool = vimdiff
[difftool]
	prompt = false
[alias]
    # Add a file/directory to your .gitignore
    ignore = "!f() { for var in "$@"; do echo \"$var\" >> .gitignore; done;}; f"
    st = status --short
    ci = commit
#[init]
  #templatedir = 
